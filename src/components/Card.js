import React from 'react';
import {motion} from 'framer-motion'

const Card = ({id,title,description}) => {
    return (
        <div
  
        >
        <h3>{title}</h3>
        <p>{description}</p>
      </div>
      );
}
 
export default Card;